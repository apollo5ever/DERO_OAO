/* const addMemberReducer = (state = ['','','','',''], action) => {
    switch(action.type){
        case 'ADDMEMBER':
            return state[action.index];
        default:
            return state;
    }
};

export default addMemberReducer; */